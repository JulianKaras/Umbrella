<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.barkerville.umbrella.Umbrella"
    tools:showIn="@layout/activity_umbrella">

    <TextView
    android:id="@+id/use_umbrella"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/use_umbrella"
    android:textSize="46sp"/>

    <Button
        android:layout_width="130dp"
        android:layout_height="50dp"
        android:text="SetAlarm"
        android:id="@+id/setAlarm"
        android:layout_below="@+id/use_umbrella"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="98dp" />

    <Button
        android:layout_width="130dp"
        android:layout_height="50dp"
        android:text="Cancel Alarm"
        android:id="@+id/cancelAlarm"
        android:layout_below="@+id/setAlarm"
        android:layout_alignLeft="@+id/setAlarm"
        android:layout_alignStart="@+id/setAlarm"
        android:layout_marginTop="32dp" />

</RelativeLayout>
